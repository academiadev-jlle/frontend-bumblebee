import { ImageGaleryDirective } from './image-galery.directive';

describe('ImageGaleryDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageGaleryDirective();
    expect(directive).toBeTruthy();
  });
});
