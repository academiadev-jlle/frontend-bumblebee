import { ImageRoundedDirective } from './image-rounded.directive';

describe('ImageRoundedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRoundedDirective();
    expect(directive).toBeTruthy();
  });
});
